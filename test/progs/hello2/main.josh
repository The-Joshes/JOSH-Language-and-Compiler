import "io" // Include everything in io.josh as if it were declared here
include "function" // Include everything in function.josh as though declared in a namespace

int main()
{
    // the compiler knows to look in standard/io to find the code for cout
    cout << "Hello, world!\n";
    
    // this will print "Have a good day, world!\n"
    function::farewell();

    return 0;
}
